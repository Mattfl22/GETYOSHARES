<div class="container">
  <h1>Projects#index</h1>
  <p>Find me in app/views/projects/index.html.erb</p>

  <% @projects.each do |project| %>
    <%= link_to project.name, products_show_path %>
  <% end %>

   <%= link_to "Back", root_path %>
</div>
