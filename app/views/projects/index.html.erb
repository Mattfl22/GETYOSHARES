<div class="container">
  <h1>Projects#index</h1>
  <p>Find me in app/views/projects/index.html.erb</p>

  <% @projects.each do |project| %>
    <%= project.name %>
    <%#= link_to project.name, project_product_path %>
  <% end %>

   <%= link_to "Back", root_path %>
</div>
