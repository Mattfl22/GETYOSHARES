<!DOCTYPE html>
<html>
  <head>
    <title>GETYOSHARES</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.4/css/all.css">
    <link rel="icon" type="image/png" href="logo.png" />
    <%= favicon_link_tag %>
    <style>
    @import url('https://fonts.googleapis.com/css2?family=Anton&family=Bungee+Shade&family=Days+One&family=Exo:wght@700&family=Russo+One&family=Wallpoet&display=swap');
    </style>
  </head>

  <body>
  <%# class header, content, footer, to have a footer always on the bottom of the page %>

    <header class="site-header"><%= render 'shared/navbar-home' %></header>

	  <main class="site-content"><%= yield %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    </main>
    
	  <footer class="site-footer"><%= render 'shared/footer' %></footer>
    

  </body>
</html>

<%# Sidebar %>
<%#  <% if params[:action] == "home" %>
      <%# <%= render 'shared/navbar-home'
    <% else
      <div class="d-flex">
        <%= render 'shared/sidebar'
        <div class="flex-grow-1">
    <% end

    <% unless params[:action] == 'home'
        </div>
      </div>
    <% end %>
